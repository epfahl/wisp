# wisp
A tiny static lisp interpreter written in Python.


## ToDo

* Better modularize code (parse, env, eval).
* Structure environment to include documentation.  Add `help` function to display doc?
* Unit tests for parser and evaluator.